// ...existing code... chart.value = markRaw(echarts.init(document.getElementById(prop.id) as
unknown as HTMLDivElement)) // 修改为 chart.value =
markRaw(echarts.init(document.getElementById(prop.id) as unknown as HTMLDivElement)) // ...existing
code...
